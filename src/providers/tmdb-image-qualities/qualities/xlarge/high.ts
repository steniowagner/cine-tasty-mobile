import { MappingImageTypeToImageSize } from '../../types';

const high: MappingImageTypeToImageSize = {
  poster: 'w500',
  backdrop: 'w780',
  still: 'w300',
  profile: 'w342',
};

export default high;
