import { MappingImageTypeToImageSize } from '../../types';

const low: MappingImageTypeToImageSize = {
  poster: 'w92',
  backdrop: 'w300',
  still: 'w185',
  profile: 'w154',
};

export default low;
