import { ImagesTypes } from 'types';

const high: Record<ImagesTypes, string> = {
  poster: 'small-high-poster',
  backdrop: 'small-high-backdrop',
  still: 'small-high-still',
  profile: 'small-high-profile',
};

export default high;
