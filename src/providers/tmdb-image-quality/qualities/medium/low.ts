import { ImagesTypes } from 'types';

const low: Record<ImagesTypes, string> = {
  poster: 'medium-low-poster',
  backdrop: 'medium-low-backdrop',
  still: 'medium-low-still',
  profile: 'medium-low-profile',
};

export default low;
