import { ImagesTypes } from 'types';

const high: Record<ImagesTypes, string> = {
  poster: 'medium-high-poster',
  backdrop: 'medium-high-backdrop',
  still: 'medium-high-still',
  profile: 'medium-high-profile',
};

export default high;
