import { ImagesTypes } from 'types';

const low: Record<ImagesTypes, string> = {
  poster: 'xsmall-low-poster',
  backdrop: 'xsmall-low-backdrop',
  still: 'xsmall-low-still',
  profile: 'xsmall-low-profile',
};

export default low;
