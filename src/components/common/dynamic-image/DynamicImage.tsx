import React from 'react';
import { View } from 'react-native';

const DynamicImage = () => (
  <View
    style={{
      width: '100%',
      height: '100%',
      backgroundColor: '#f0f',
    }}
  />
);

export default DynamicImage;
