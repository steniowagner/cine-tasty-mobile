import styled from 'styled-components/native';

export const ImagesListWrapper = styled.View`
  margin-bottom: ${({ theme }) => theme.metrics.lg * 2}px;
`;

export const CastGap = styled.View`
  width: 1px;
  height: ${({ theme }) => theme.metrics.xl}px;
`;
